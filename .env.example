# 법제처 OPEN API 인증: 신청 이메일의 @ 앞부분. 별도 발급 키 없음.
LAW_OC=your_email_id

DATABASE_URL=postgresql://localhost/lawrag

# model2vec: torch 불필요한 정적 임베딩 (EMBED_DIM 은 모델이 결정)
EMBED_MODEL=minishlab/potion-multilingual-128M
EMBED_DIM=256

# DRF 호출 간격(초). 0.3 미만으로 낮추지 말 것 — 이용 제한 경고 있음.
DRF_SLEEP=0.5
